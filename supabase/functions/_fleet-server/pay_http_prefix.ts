/**
 * HTTP path prefix for fleet-pay domain controllers (settlements, periods, disputes, ledger lines).
 * Standalone fleet-pay wraps with Hono `.basePath("/fleet-pay")` (ADR-0021 F4).
 */
export const PAY_HTTP_PREFIX = "";
