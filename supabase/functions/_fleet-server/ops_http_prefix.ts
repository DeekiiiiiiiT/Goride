/**
 * HTTP path prefix for maintenance + expense-hub register hooks on fleet-ops.
 * Standalone fleet-ops wraps with Hono `.basePath("/fleet-ops")` (ADR-0020 / ADR-0021 F2).
 */
export const OPS_HTTP_PREFIX = "";
