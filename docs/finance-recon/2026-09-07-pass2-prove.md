# Pass 2 — prove close (H-4 / H-7 / H-9) — 2026-09-07

## H-4 verify-on-read
- `verifyPeriodCloseHash` / `storedCloseHashFromPeriod` in `packages/finance-core/src/closeHash.ts`
- Frozen period GET (`getDriverFinancialPeriodDetail`) recomputes hash; mismatch → `metadata.financeCore.hashMismatch`
- Collect / pay / write-off call `assertFrozenPeriodHashIntact` → `409 HASH_MISMATCH`

## H-7 nightly
- `finance-recon` loads `week_statements` and runs `checkCloseInvariants` per period
- Missing lanes → `CLOSE_STATEMENT_MISSING` (warning); blocks → critical when severity=block
- Also M-2: `FUEL_EVENT_MISSING_ACCOUNTS` when fuel events lack debit/credit keys

## H-9 canonical toll charged
- DFP prefers `toll_charged_to_driver` / `toll_charge_reversed` financial_events
- KV `Toll Charge` txs only when no canonical events and `PROJECTION_EVENTS_TOLLS` is off
- `projectionSources.tollCharged`: `events` | `kv`
