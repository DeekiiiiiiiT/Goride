# Copy to keystore.properties (same folder) and fill in your passwords.
# keystore.properties is gitignored — never commit it.
#
# Create a new upload keystore for Roam Rush (separate Play app).
storeFile=C\:\\Users\\deeki\\roam-rush-upload-key.jks
keyAlias=roam-rush
storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
