/**
 * @deprecated Split fills no longer collect a typed cash portion.
 * Cash = pump total − Dominion statement card amount after match.
 * Kept as empty module so old imports fail loudly if reintroduced.
 */
export {};
