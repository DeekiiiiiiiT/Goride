/** Mirrors server MIN_FORCE_APPROVE_REASON_LENGTH in complianceLogic.ts */
export const MIN_FORCE_APPROVE_REASON_LENGTH = 10;

/** Default audit reason when super admin force-activates (testing / override). */
export const DEFAULT_FORCE_ACTIVATE_REASON = 'Testing — skip compliance verification';
