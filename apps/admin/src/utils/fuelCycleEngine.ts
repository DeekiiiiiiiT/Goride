/**
 * Dominion re-exports @roam/fuel-core fuel cycle engine (Phase 1 Dominion/Fleet decoupling).
 * Do not reintroduce a local fork.
 */
export { calculateFuelCycles } from '@roam/fuel-core';
export type { FuelCycle } from '../types/fuel';
