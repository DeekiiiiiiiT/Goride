# Stitch design reference — Fleet Policy Configuration

Project: **Roam Fleet APP** (`3587123323600813385`)
Design system: **Precision Operations** (`assets/d393c21ec4be4c768bf9a2e8060fd674`)

## Screens generated (Phase 1)

| Screen | Purpose |
|--------|---------|
| Fleet Policy Configuration (desktop) | Policy list with company/driver matrix + assigned vehicles |
| Fleet Policy Configuration (Mobile) | Mobile stacked cards |
| Edit Policy Modal | Percentage company/driver editor (no receipt/cap) |
| Assign vehicles to Owner Operator | Multi-select vehicle assignment sheet |

Assets downloaded via Stitch `download_assets` into this folder for implementation reference.
React implementation lives under `apps/fleet/src/components/fuel/` (`ScenarioList`, `FuelCoverageMatrix`, `AssignVehiclesToPolicySheet`, `ScenarioEditor`, `FuelConfiguration`).
