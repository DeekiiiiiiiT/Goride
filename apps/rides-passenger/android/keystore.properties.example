# Copy to keystore.properties (same folder) and fill in your passwords.
# keystore.properties is gitignored — never commit it.
#
# Use the same upload keystore you used for the first Play Console upload.
storeFile=C\:\\Users\\deeki\\roam-driver-upload-key.jks
keyAlias=roam-driver
storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
