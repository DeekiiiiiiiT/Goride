# Copy to keystore.properties (same folder) and fill in your passwords.
# keystore.properties is gitignored — never commit it.
#
# Create a new upload keystore for Roam Courier (separate Play app from Driver).
storeFile=C\:\\Users\\deeki\\roam-courier-upload-key.jks
keyAlias=roam-courier
storePassword=YOUR_KEYSTORE_PASSWORD
keyPassword=YOUR_KEY_PASSWORD
